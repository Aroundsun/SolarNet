# 2026-07-02

## 18:25 - SolarNet 项目启动 & 需求对齐

- 用户明确 SolarNet 定位：长期维护的现代 C++20 Linux 网络框架，参考 muduo 思想但不照搬，作为 Solar 系列底层基础设施（云盘、RPC、任务调度、HTTP、AI Gateway）。
- 统一规范：C++20、CMake、clang-format、clang-tidy；命名 PascalCase / m_xxx / camelCase / kXXX / namespace solar_net；RAII，禁止裸 new/delete，优先现代设施。
- 开发流程：需求分析 → 模块职责 → 接口设计 → 类图 → 生命周期 → 流程图 → 边界情况 → 性能分析 → 编码 → 单测 → Benchmark → 文档。
- 设计原则：SOLID、RAII、KISS、DRY、高内聚低耦合、组合优于继承、接口优于实现；类 < 500 行、cpp < 800 行；每模块必须配 GoogleTest + Example。
- 完成身份档案初始化：助手名 Sol，用户 xhy，西安财经大学 C++ 后端方向，Solar 系列项目Owner。
  - 下一步：用户确认首个模块（建议从 Timestamp / Logger / Buffer 等基础模块开始，并给出模块级 12 步方案）。

## 18:55 - 目录结构约束调整

- 用户要求：头文件和源文件放在同一目录下，不要 `include/` 和 `src/` 分开。
- 已调整：删除 `include/` 和 `src/`，创建 `solar_net/` 目录，将 `version.h` 和 `version.cpp` 移至 `solar_net/`。
- 已更新 `CMakeLists.txt`：
  - `SOLAR_NET_PUBLIC_DIR` 指向项目根目录，使外部代码包含 `solar_net/xxx.h`。
  - `SOLAR_NET_SOURCE_DIR` 指向 `solar_net/`，统一收集 `.h` 和 `.cpp`。
  - 安装规则改为 `install(DIRECTORY solar_net DESTINATION ...)`。
- 已更新 `README.md`：新增 Project Layout 说明。
- 已写入长期记忆 `MEMORY.md`：SolarNet 项目约定中明确源码布局为同目录 `solar_net/`。

## 18:38 - Phase 0 工程基础设施搭建

- 设计并创建目录结构：include/solar_net、src、tests、benchmarks、examples、cmake、.github/workflows。
- 编写根 CMakeLists.txt：C++20、编译警告、clang-tidy 集成、静态库目标、FetchContent 拉取 GoogleTest 和 Google Benchmark。
- 编写 tests/CMakeLists.txt、benchmarks/CMakeLists.txt、examples/CMakeLists.txt，统一 `solar_net_add_*` 辅助函数。
- 创建 .clang-format（LLVM 基准 + 列宽 100、Attach brace、4 空格缩进）。
- 创建 .clang-tidy（bugprone / clang-analyzer / cppcoreguidelines / google / misc / modernize / performance / portability / readability 等）。
- 创建 .github/workflows/ci.yml：Ubuntu + gcc/clang + Debug/Release 矩阵，编译、单测、Benchmark。
- 创建最小可运行骨架：version.h / version.cpp / example_version.cpp / test_version.cpp / bench_version.cpp。
- 当前环境限制：Windows 宿主未安装 CMake/g++/clang-tidy，WSL 需要更新，无法直接编译验证。已在 WSL 尝试但失败，需用户在 Ubuntu VM 中执行构建命令并反馈结果。
- 提供构建命令：
  ```bash
  cmake -B build -S . -DCMAKE_BUILD_TYPE=Release -DSOLAR_NET_USE_CLANG_TIDY=OFF
  cmake --build build --parallel
  ctest --test-dir build --output-on-failure
  ./build/benchmarks/bench_version
  ./build/examples/example_version
  ```
